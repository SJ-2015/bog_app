<!-- 
create a form for creaturse, URL, method do|f|

name

Descri

save
?? how to decide which URL to post to?

is current way better than just posting?
-->

<%= form_for @creature do |f| %>

	<%= f.text_field :name %>
	<%= f.text_area :description %>
	<%= f.submit "save creature" %>
<% end %>
